# Email Configuration for Grazin Acres
# Copy this file to .env and update with your actual email credentials

# Email Service Provider Settings
EMAIL_USER=your-farm-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
ADMIN_EMAIL=admin@grazinacres.com

# SMTP Settings (if using custom email provider)
SMTP_HOST=smtp.yourwebhost.com
SMTP_PORT=587

# Application Settings
NODE_ENV=development
PORT=3000

# Instructions for setting up email:

# FOR GMAIL:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an "App Password" for this application
# 3. Use your Gmail address for EMAIL_USER
# 4. Use the app password (not your regular password) for EMAIL_PASSWORD

# FOR OUTLOOK/HOTMAIL:
# 1. Use your Outlook email for EMAIL_USER
# 2. Use your regular password for EMAIL_PASSWORD
# 3. May need to enable "Less secure app access" in Outlook settings

# FOR CUSTOM HOSTING (Recommended for production):
# 1. Contact your web host for SMTP settings
# 2. Usually they provide: SMTP server, port, username, password
# 3. Update SMTP_HOST, SMTP_PORT, EMAIL_USER, EMAIL_PASSWORD accordingly

# Security Notes:
# - Never commit this file with real credentials to version control
# - Add .env to your .gitignore file
# - Use environment variables in production hosting
